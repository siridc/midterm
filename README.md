This is a simple PHP + MySQL project that demonstrates basic CRUD operations (Create and Read only for now).
It allows you to insert student records into a MySQL database and view them in a styled HTML table.
